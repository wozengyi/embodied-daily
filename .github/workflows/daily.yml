name: Daily Papers Build & Deploy

on:
  schedule:
    - cron: '20 0 * * *'
  workflow_dispatch:
    inputs:
      backfill_years:
        description: 'Set >0 to run a multi-year backfill before the daily build'
        required: false
        default: '0'
  push:
    branches: [main]
    paths:
      - '**.py'
      - '**.js'
      - '**.html'
      - '**.css'
      - '.github/workflows/*.yml'

permissions:
  contents: write
  pages: write
  id-token: write
  issues: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: (Optional) Backfill multi-year history
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.backfill_years != '0' }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 2
          timeout_minutes: 60
          retry_on: error
          command: python build/backfill_years.py --years ${{ github.event.inputs.backfill_years }} --max-per-month 200 --page-size 100

      - name: Generate daily data (with retry)
        uses: nick-fields/retry@v3
        with:
          max_attempts: 3
          timeout_minutes: 25
          retry_on: error
          command: python build/build_daily.py --recent 7 --archive 1825 --archive-limit 800 --venue-limit 40

      - name: Warn on suspicious builds
        run: |
          python - <<'PY'
          import json, os, urllib.request, sys
          from pathlib import Path
          p = Path('data/daily.json')
          data = json.loads(p.read_text(encoding='utf-8'))
          warns = list(data.get('warnings') or [])
          if (data.get('count') or 0) < 20:
              warns.append('low_paper_count')
          if warns:
              print('Warnings:', warns)
              body = (
                f"## Daily papers build warnings\n\n"
                f"- Generated at: `{data.get('generatedAt')}`\n"
                f"- Latest (7 days): {data.get('count')} (HF={data.get('sources',{}).get('hf')}, arXiv={data.get('sources',{}).get('arxiv')})\n"
                f"- Archive: {data.get('archiveCount')}\n"
                f"- History total: {data.get('historyTotal')}\n"
                f"- Added today: {data.get('addedToday')}\n\n"
                f"Warnings: {', '.join(warns)}\n\n"
                f"Trigger a manual workflow run with a `backfill_years` parameter to re-seed history if needed."
              )
              token = os.environ.get('GITHUB_TOKEN')
              repo = os.environ.get('GITHUB_REPOSITORY')
              if token and repo:
                  import urllib.parse
                  q = urllib.parse.urlencode({'labels':'daily-builder','state':'open'})
                  req = urllib.request.Request(
                      f'https://api.github.com/repos/{repo}/issues?{q}',
                      headers={'Authorization': f'Bearer {token}', 'Accept':'application/vnd.github+json'}
                  )
                  try:
                      with urllib.request.urlopen(req, timeout=15) as r:
                          open_issues = json.loads(r.read())
                      if any(('Daily papers build warnings' in (i.get('title') or '')) for i in open_issues):
                          print('Warning issue already open; skipping.')
                          sys.exit(0)
                      payload = json.dumps({'title':'Daily papers build warnings','body':body,'labels':['daily-builder']}).encode()
                      req2 = urllib.request.Request(
                          f'https://api.github.com/repos/{repo}/issues',
                          data=payload, method='POST',
                          headers={'Authorization': f'Bearer {token}', 'Accept':'application/vnd.github+json',
                                   'Content-Type':'application/json'}
                      )
                      with urllib.request.urlopen(req2, timeout=15) as r:
                          iss = json.loads(r.read())
                      print('Opened issue:', iss.get('html_url'))
                  except Exception as e:
                      print('Failed to create issue:', e)
          else:
              print('Build healthy: count=', data.get('count'), 'archive=', data.get('archiveCount'))
          PY

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No new data to commit."
          else
            git commit -m "chore: update daily papers $(date -u +'%Y-%m-%d')"
            git push
          fi

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Sync latest generated data
        run: git pull --ff-only origin main
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
